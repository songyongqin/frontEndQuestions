// function greeter(person) {
//   return "Hello, " + person;
// }

// let user = "Jane User";

// document.body.innerHTML = greeter(user);

// let a : number;
// a = 'hello';

enum Gender{
  Male,
  Femanle
}

let i : {name: string, gender: Gender}

i = {
  name: 'jcak',
  gender: Gender.Male
}